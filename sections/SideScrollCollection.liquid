{% assign collection_handle = section.settings.Collection %}
{% assign product_limit = section.settings.Limit %}

{% include 'SideScroll' %}

{% schema %}
  {
    "name": "Side Scroll Collection",
    "settings": [
      {
        "id": "Collection",
        "type": "collection",
        "label": "Collection"
      },
      {
        "id": "Limit",
        "type": "number",
        "label": "Product Limit"
      }
    ],
    "presets": [
      {
        "name": "Collection",
        "category": "Side Scroll Collections"
      }
    ]
  }
{% endschema %}